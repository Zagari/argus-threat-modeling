# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Env / secrets
.env
*.local

# Node / frontend
node_modules/
frontend/dist/
frontend/.vite/

# Avaliação (Fase 5): cache de execuções e resultados (regeneráveis; o capítulo versiona só o que precisar)
eval/results/
.ipynb_checkpoints/

# ML artefatos (vão para HF Hub / Releases, não para o git)
training/runs/
runs/
training/.venv-train/
.venv-ml/
*.tgz
*.tar.gz
*.pt
*.onnx
# Dataset sintético é reproduzível (gerado por generate_synthetic.py) → fora do git
data/synthetic/
data/real/images/
data/real/labels/
data/knowledge/raw/
# Ícones oficiais AWS/Azure/GCP: download manual (licença, arquivos grandes) → não versionar
data/icons/
training/icons/*.zip
training/icons/_extracted/

# Vector store / grafo
*.sqlite3
chroma/
neo4j/data/

# OS
.DS_Store
Thumbs.db

# Relatórios gerados em runtime
*.pdf

# Documentos de planejamento/trabalho (mantidos localmente, fora do repo)
/PROPOSTA-TECNICA.md
/PLANO-IMPLEMENTACAO.md
/docs/
