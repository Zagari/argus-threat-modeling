# API
fastapi
uvicorn[standard]
python-multipart
pydantic>=2.6
pydantic-settings
python-dotenv

# LLM (camada de abstração multi-provider)
litellm

# Imagem / relatório
pillow
jinja2
weasyprint

# Taxonomia (lê o mapeamento.yaml no caminho por rótulo do E2)
PyYAML

# HTTP
httpx

# Dev / testes
pytest
ruff
mypy
