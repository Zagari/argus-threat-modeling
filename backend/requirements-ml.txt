# Extra OPCIONAL para servir o estágio E1 (detector YOLO) a partir do backend.
# Pesado (torch) e exige Python 3.12 — NÃO instalar no backend de produção 3.14.
# Use num ambiente de ML dedicado:
#   python3.12 -m venv .venv-ml && source .venv-ml/bin/activate
#   pip install -r requirements.txt -r requirements-ml.txt
# e aponte o modelo via ARGUS_DETECTOR_WEIGHTS=/caminho/best.pt
# ou ARGUS_DETECTOR_HF=Zagari/argus-detector
ultralytics>=8.3.0
huggingface_hub>=0.24

# OCR do E2 (caminho por rótulo). EasyOCR usa torch (já vem com o ultralytics) e instala
# limpo em macOS/Linux. PaddleOCR é mais preciso (use no servidor Linux), mas o
# PaddlePaddle dá trabalho no Apple Silicon — por isso fica opcional/comentado.
easyocr>=1.7
# paddleocr>=2.7   # primário no servidor Linux (requer paddlepaddle)
# paddlepaddle>=2.6

# RAG semântico (E5 híbrido + busca do Knowledge Explorer) — embeddings LOCAIS.
# Opcional/gracioso: sem estas libs (ou ARGUS_RAG!=1), tudo cai no determinístico/substring.
chromadb>=0.5
sentence-transformers>=3.0

# Backend de grafo opcional (Graph-RAG, 3.8) — só com ARGUS_KG_BACKEND=neo4j; sem o driver
# (ou sem instância) o get_store() cai no LocalKG. Driver puro-python, leve.
neo4j>=5.0
